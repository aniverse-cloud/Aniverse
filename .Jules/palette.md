## 2024-07-02 - Initial Setup\n**Learning:** This is an Android app using Jetpack Compose, not a web app. Commands like `pnpm` and standard web ARIA labels don't apply directly. We need to translate UX principles to Compose (e.g., using `contentDescription`, semantic modifiers, or proper typography/spacing in Compose).\n**Action:** Use Android Compose equivalents for UX improvements.
